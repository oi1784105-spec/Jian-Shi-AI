# 演示模式构建参数（`npm run build:demo`）。
#
# 该模式产出一个纯静态站点：所有接口由浏览器本地的模拟层接管
# （见 src/demo），不依赖 FastAPI 后端，可直接托管在 GitHub Pages。
VITE_DEMO_MODE=true

# 发布子路径。GitHub Pages 项目站点地址为 https://<user>.github.io/<repo>/，
# 因此 base 必须是 /<repo>/；换仓库名时同步修改这里和部署工作流。
VITE_BASE=/Jian-Shi-AI/
